{
  "name": "@cargable/backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch dev-server.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "build": "tsc --noEmit",
    "build:vercel": "tsc --noEmit && node scripts/build-vercel.mjs"
  },
  "dependencies": {
    "@cargable/contracts": "workspace:*",
    "@supabase/supabase-js": "2.112.3",
    "hono": "4.13.1",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@hono/node-server": "2.1.1",
    "esbuild": "0.28.2",
    "tsx": "4.23.12"
  }
}
